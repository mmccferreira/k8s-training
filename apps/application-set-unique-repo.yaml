apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: root
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - list:
        elements:
          - name: simple-app
            path: charts/simple-app
            type: api
          - name: app-with-db
            path: charts/app-with-db
            type: api
          - name: postgres
            path: charts/postgres
            type: services
  template:
    metadata:
      name: '{{.name}}'
      labels:
        type: '{{.type}}'
    spec:
      project: default
      source:
        helm:
          valueFiles:
            - values.yaml
        # TODO: Replace by your username and push the changes to your fork
        repoURL: git@github.com:mmccferreira/k8s-training.git
        targetRevision: argocd # This is the name of the branch where we have our code
        #targetRevision: HEAD # This is the proper value when configuring it normally
        path: '{{.path}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
      destination:
        server: https://kubernetes.default.svc
        namespace: dev
